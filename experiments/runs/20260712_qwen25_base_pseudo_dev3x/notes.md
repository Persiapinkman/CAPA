# Run Notes

Legacy prompt-format reference arm. All 49 rows produced parseable first JSON, but every raw completion contained tail text. This arm is retained only to isolate the ChatML format effect.
