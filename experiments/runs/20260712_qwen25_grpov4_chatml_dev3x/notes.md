# Run Notes

GRPOv4 remains cleanly terminating and improves several migration step scores, but its case-macro effect versus SFTv3 is inconclusive and `full_detection_eval` regresses from 1.00 to 0.75. The adapter remains a diagnostic artifact, not the active validated configuration.
