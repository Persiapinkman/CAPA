# Run Notes

Native ChatML removed raw tail text on all 49 rows in all three repeats while preserving JSON validity. Routing-score change versus pseudo tags is statistically inconclusive, but ChatML is promoted for stopping correctness and model-template compatibility.
