# Archived Predecessor Snapshot

The original vendored `test-skills` repository snapshot was moved out of Git during the 2026-07-12 repository restructuring.

Physical location:

```text
/raid/zkq/artifacts/CAPA/snapshots/test_skills_original
```

The snapshot remains read-only historical material. Active CAPA source, datasets, and evaluation fixtures live in their top-level versioned directories; new training code must not import from this snapshot.
